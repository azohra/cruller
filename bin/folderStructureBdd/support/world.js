const cruller = require('../../../lib/bdd');
// Custom settings object which will override comiconfig and default settings
const customSettings = {};
// Initialize cucumber world
cruller(customSettings);
